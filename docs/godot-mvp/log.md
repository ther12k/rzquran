# Bundle update log

## 2026-09-07
- **Creation**: Prepared the scoped Godot MVP, 32 issue documents, technical contracts and evidence rules.
- **Format**: Organized all documents as an OKF v0.2 Markdown-only bundle.
- **Boundary**: No repository implementation, live GitHub registration, human approval or application test results are implied.
- **Placement decision (ADR-002)**: Owner directed that the MVP client and this documentation live in a new dedicated repository; `rz-quran` remains the sole backend with the React parent/admin areas. Issue documents left unchanged so recorded hashes stay valid.
- **Baseline inspection**: `rz-quran` verified on `main` at `f1b43db` (clean, synced); reported SHA `669c5e3` present in history.
- **Design references**: Added six owner-provided UI mockups under [design/mockups](design/mockups/README.md). Visual direction only; not authoritative content, permissions, or scope.
- **GDM-001 (QA-01)**: Inspected `rz-quran` @ `f1b43db` and committed the [baseline inventory](delivery/gdm-001-baseline-inventory.md): 6 proposed operations map to existing routes, 2 partially, 4 are gaps (media byte route, abandon, and the staging pairing set). All four backend test suites re-executed green (46 tests). `/api/v1/media/stream/*` confirmed missing; no feature flags exist.
- **GDM-004 (QA-05)**: Committed the [technical threat review](delivery/gdm-004-threat-review.md) covering all 11 security-checklist rows, six resolved design contradictions (TTL, answer-replay semantics, idempotency mapping, playback token, feature flag, bootstrap composition), configurable limits, and five specified production-rejection tests. Tests specified, not yet executed.
- **GDM-002 (QA-02) + GDA-001**: Godot 4.7.2.stable pinned (sha256-verified editor + templates). Shared shell committed at `2234243d4569`: web export, Linux smoke binary, and Android gradle debug APK built from that one SHA with visible build ID `2234243d4569-20260907T222815Z`; native smoke run passed (`platform=native-https`). [Runbook](delivery/gdm-002-build-runbook.md) and [evidence](delivery/gdm-002-evidence.md) committed with artifact hashes. On-device QA-31/32/37 and audio playback remain unexecuted.
- **Asset starter v1.1 merged**: `asset-starter/` added byte-identical (33 files); the 76 original bundle docs verified unchanged against v1.0. Theme + three UI WAVs adopted into `game/` (hashes match the registry); standalone preview imported clean in the pinned editor and stays separate from the game. GDA-002/003 (custom artwork, asset verification) remain open proposals; no GDA issues registered on GitHub (not authorized).
